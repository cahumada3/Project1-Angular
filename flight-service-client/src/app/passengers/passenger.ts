export interface Passenger {
    id: number; 
    firstName: string;
    lastName: string; 
    email: string;
}

export interface PassengerDTO {
    firstName: string;
    lastName: string; 
    email: string;
}
